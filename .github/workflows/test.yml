# This file intentionally has linting errors for testing

name: Test Workflow  

on:
  push:
   branches:  
     - main
  pull_request:
    branches:
      - main
      
jobs:
  test:
   runs-on: ubuntu-latest
   steps:
      - uses: actions/checkout@v999
      - name: Run tests
        run: |
          echo "This line is way too long and exceeds the maximum line length of 120 characters which will trigger a yamllint warning or error depending on configuration"
          echo "test"
